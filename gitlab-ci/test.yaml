variables:
  COMPOSER_CACHE_DIR: "/cache/composer"
  COMPOSER_ALLOW_SUPERUSER: "1"
  COMPOSER_NO_INTERACTION: "1"

before_script:
  - composer self-update
  - composer global require hirak/prestissimo --no-progress

stages:
  - test

.test: &test-template
  stage: test
  image: webdevops/php:${PHP_VERSION}
  before_script:
    - pecl install pcov
    - docker-php-ext-enable pcov
  script:
    - >
      if [[ $TEST_COMPATIBILITY ]]; then
        composer require --dev pcov/clobber
        vendor/bin/pcov clobber
      fi
    - composer req --dev eliashaeussler/composer-update-check:"${UPDATE_CHECK_VERSION}" --no-progress
    - COMPOSER_PROCESS_TIMEOUT=1000 composer test:ci -- --testdox
  coverage: '/^\s*Lines:\s*\d+.\d+\%/'
  artifacts:
    reports:
      junit: .build/coverage/junit.xml
